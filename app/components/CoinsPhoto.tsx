/** Photo corners belonged to the paper theme; the sky theme has none. Kept so callers need no change. */
export function CoinsPhoto(_props: { wide?: boolean; animate?: boolean; hollow?: boolean }) {
  return null;
}
