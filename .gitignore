# Foundry
contracts/out/
contracts/cache/
contracts/broadcast/
contracts/lib/**/.git

# Node
node_modules/
.next/
dist/

# Secrets — jamais de clé en clair dans le dépôt (SECURITY.md)
.env
.env.*
!.env.example

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/

# Next.js
app/.next/
app/next-env.d.ts
app/.env.local
# Keeper
keeper/hunt-secrets.json

# Build outputs of the alternate Next.js dist dirs
app/.next-*/
# Session tooling, not part of the product
.agents/
.claude/
skills-lock.json
# Models are downloaded by server/scripts (see DEPLOY.md), media is runtime data
server/models/*.onnx
server/media/
server/*.log
# Video sources and intermediates: only the finished cuts are kept
brand/video/*.y4m
brand/video/*.webm
brand/video/*.wav
brand/video/map-frames/
brand/video/clip*.mp4
brand/video/pov*.mp4
brand/video/w?.mp4
brand/video/tesla.mp4
brand/video/app-*.mp4
brand/video/spot-teaser.mp4
*.tsbuildinfo

# Vercel CLI link
.vercel/
app/.vercel-deploy.log
contracts/admit.log
